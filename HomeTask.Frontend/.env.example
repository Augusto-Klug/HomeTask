# URL base da API backend (ex: http://localhost:5000)
VITE_API_BASE_URL=http://localhost:5000

# Troque para "true" para usar dados fictícios (MSW) em vez do backend real.
# As chamadas aparecem normalmente no DevTools → Network.
VITE_USE_MOCK=false
